<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= post.title %> - Blog Details</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <h1><%= post.title %></h1>
        <% if (post.image) { %>
            <img src="/uploads/<%= post.image %>" alt="Blog Image" class="blog-image">
        <% } %>
        <p class="content"><%= post.content %></p>
        <p class="author">Written by: <%= post.author %></p>
        <a href="/" class="btn">Back to Home</a>
        <a href="/posts/<%= post._id %>/edit" class="btn edit-btn">Edit</a>
        <form action="/posts/<%= post._id %>?_method=DELETE" method="POST" class="delete-form">
            <button type="submit" class="btn delete-btn">Delete</button>
        </form>
    </div>
</body>
</html>
